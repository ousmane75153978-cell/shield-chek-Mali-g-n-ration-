name: shieldcheck_agent
description: Application ShieldCheck pour la securite des appareils.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  flutter_secure_storage: ^9.0.0

flutter:
  uses-material-design: tr
  ue
